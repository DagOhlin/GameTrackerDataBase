# Game Tracker Database

## Prerequisites
 install the required libraries using pip: should, panda is needed by sreamlit to draw out the tables 
`pip install -r requirements.txt`

## Password
On line 18 in app.py just swap secretPasword123 for your slq instance password


## How to Run GameTracker
This will startt the web interface
`streamlit run python/app.py`
or try this if first one does not work
`python3 -m streamlit run python/app.py`
